{
  "name": "student-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Full stack MERN Student Task Dashboard Management System",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix client",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix client\"",
    "server": "npm run dev --prefix backend",
    "client": "npm run dev --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
